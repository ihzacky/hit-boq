<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_order_inherit_form_view" model="ir.ui.view">
        <field name="name">sale.order.inherit.form.view</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="is_boq" invisible="0"/>
                <field name="boq_id" readonly="1" invisible="not is_boq"/>
                <field name="boq_name" readonly="1" invisible="not is_boq"/>
            </xpath>

            <xpath expr="//button[@id='create_invoice']" position="after">
                <button name="%(project.open_create_project)d" 
                        string="Create Project" 
                        type="action" 
                        class="btn-primary"
                        context="{'default_partner_id': partner_id, 'default_name': boq_name}"
                        invisible= "not is_boq"
                />
            </xpath>
        </field>
    </record>
</odoo>